// Placeholder — P1 (Fee setup) and P2 (Dashboard) will replace this entirely.
export default function FinanzasPage() {
  return (
    <div className="flex h-full items-center justify-center p-8 text-zinc-400">
      Finanzas — próximamente
    </div>
  );
}
